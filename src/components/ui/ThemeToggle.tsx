// Theme is forced to dark globally. Render a static dark indicator instead
// of an interactive toggle to avoid implying the theme can be changed.
export default function ThemeToggle() {
  // Theme indicator removed — render nothing so no button appears in the UI.
  return null;
}
